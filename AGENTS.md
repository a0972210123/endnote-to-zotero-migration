# AI agent 指示（Codex / Claude Code / 其他）

這個 repo 的用途是**把使用者的 EndNote library 搬到 Zotero**，
文獻、PDF 附件與分組階層都要完整保留。

## 你要做的事

**照著 [`.claude/skills/endnote-to-zotero/SKILL.md`](.claude/skills/endnote-to-zotero/SKILL.md) 走。**
那份是完整的作業流程與安全規則，先讀完再開始。

搭配的使用者手冊在 [`docs/02_遷移手冊.md`](docs/02_遷移手冊.md)，
使用者卡住時可以指給他看對應章節。

## 使用者是誰

**多半不是工程背景的研究生**，而且這是他們累積多年的文獻資產。

- 說明用白話，每一步講清楚「現在做什麼、為什麼、會發生什麼」
- **一次講一步**，等他做完回報再往下，不要一口氣丟九個步驟
- 他說「看不懂」時換更白話的講法，不要重複同一段

## 🔴 絕對不可違反

1. **EndNote 原庫只讀不寫。** 不修改、移動、刪除 `.enl` 或 `.Data` 內任何既有檔案。
2. **偵測，不要猜路徑。** 絕不假設 library 在 `D:\` 或任何預設位置。
3. **找到多份 library 就停下來問人**，不要自己挑「看起來最新的」。
4. **每個破壞性動作前要人確認**（合併重複、刪 note、批次改名）。
5. **驗收看數字，不看腳本說「成功」。** 每個關卡都要和使用者記下的 EndNote 原始數字核對。
6. **不確定就停。** 寧可多問一句，不要弄壞使用者的文獻庫。

## 環境：開場就要問完的五項

**不要邊做邊發現。** 第 3、4 項若不合格，是走到步驟 3 和步驟 7 才會炸——
那時使用者已經匯入好幾千筆了。

| # | 檢查 | 不合格怎麼辦 |
|---|---|---|
| 1 | 作業系統 | **macOS 路徑 A 可行**，但指令改 `python3`／正斜線／免 PYTHONIOENCODING；尚未實測過，要誠實說明 |
| 2 | Python 3.8+（Mac 用 `python3`） | 走純 GUI 的「路徑 B」，指向手冊第 5 章，**不要強推安裝** |
| 3 | EndNote ≥ X9.3 | 更舊的 library 不是 SQLite，腳本讀不到 → 只能走路徑 B |
| 4 | Zotero ≥ 7（有 `Tools → Developer → Run JavaScript`） | **請他先更新 Zotero**，路徑 A 第 7 步完全靠這個選單 |
| 5 | Zotero 已安裝並登入 | 先去註冊 |

**第 3、4 項不合格時，使用者仍可完整走完路徑 B——要主動這樣講**，
不要讓他以為自己沒救了。

其他：Windows 命令列中文報錯 → 加 `PYTHONIOENCODING=utf-8`。
